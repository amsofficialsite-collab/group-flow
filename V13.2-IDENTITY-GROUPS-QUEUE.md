# GROUP FLOW V13.2 — Identity-specific Groups + Better Daily Queue

## สิ่งที่เปลี่ยน

- เพิ่ม `facebook_identities` สำหรับ Facebook Profile / Facebook Page
- เพิ่ม `group_identity_access` เป็น many-to-many mapping ระหว่าง Identity กับ Group
- หน้า Groups สามารถสร้าง Identity และกำหนดว่า Identity ใดเข้าถึง Group ใดได้
- หน้า Daily Queue ต้องเลือก Identity ก่อน แล้วจะแสดงเฉพาะ Groups ที่ Identity นั้นใช้ได้
- Queue เก็บ `identity_id` พร้อม snapshot `post_as` / `posting_identity`
- Scheduler ตรวจ mapping อีกครั้งก่อนส่งงานให้ Extension ป้องกันโพสต์ผิด Identity/Group
- คิวตกค้างแสดงรายละเอียด พร้อม `โพสต์ตอนนี้`, `เลื่อนเวลา`, `ยกเลิก`
- งานที่ยกเลิกถูกเปลี่ยนเป็น `skipped` และบันทึก Posting History
- Posting History เพิ่มคอลัมน์ Identity
- แก้ duplicate Posting History: ผลจาก Extension ถูกบันทึกโดย `/api/posting/finish` เพียงจุดเดียว

## วิธีอัปเกรดจาก V13.1

1. Deploy source V13.2
2. รัน `supabase/v13_2_identity_groups.sql` ใน Supabase SQL Editor หนึ่งครั้ง
3. เปิดหน้า Groups
4. ตรวจ Facebook Identities ที่ migration สร้างจากข้อมูลเดิม
5. เพิ่ม/แก้ชื่อ Profile และ Page ตามจริง
6. เข้าแต่ละ Group แล้วติ๊ก Identity ที่เข้าถึง Group นั้นได้
7. Reload Chrome Extension V13.2
8. สร้าง Queue ใหม่: เลือก Identity → Group → Content → Schedule

> Migration ไม่ลบ Groups, Queue หรือ Posting History เดิม

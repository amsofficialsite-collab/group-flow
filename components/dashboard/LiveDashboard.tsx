"use client";import{useEffect,useState}from"react";import{createBrowserClient}from"@supabase/ssr";import{CalendarClock,FileText,Send,Users}from"lucide-react";
export default function LiveDashboard(){const s=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!);const[counts,setCounts]=useState({groups:0,contents:0,queue:0,posted:0});const[recent,setRecent]=useState<any[]>([]);useEffect(()=>{(async()=>{const start=new Date();start.setHours(0,0,0,0);const end=new Date();end.setHours(23,59,59,999);const[g,c,q,p,r]=await Promise.all([s.from("groups").select("id",{count:"exact",head:true}),s.from("content_items").select("id",{count:"exact",head:true}),s.from("queue_items").select("id",{count:"exact",head:true}).gte("scheduled_at",start.toISOString()).lte("scheduled_at",end.toISOString()),s.from("posting_logs").select("id",{count:"exact",head:true}).eq("result","posted").gte("posted_at",start.toISOString()),s.from("groups").select("id,name,category,created_at").order("created_at",{ascending:false}).limit(5)]);setCounts({groups:g.count||0,contents:c.count||0,queue:q.count||0,posted:p.count||0});setRecent(r.data||[])})()},[]);const cards=[["กลุ่มทั้งหมด",counts.groups,Users],["คอนเทนต์",counts.contents,FileText],["คิววันนี้",counts.queue,CalendarClock],["โพสต์สำเร็จวันนี้",counts.posted,Send]] as const;return <div className="space-y-7"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,n,I])=><div className="card" key={label}><I className="text-cyan-300"/><p className="mt-6 text-4xl font-black">{n}</p><p className="mt-1 text-sm text-white/50">{label}</p></div>)}</div><div className="card"><h2 className="text-lg font-bold">กลุ่มที่เพิ่มล่าสุด</h2><div className="mt-4 divide-y divide-white/10">{recent.map(r=><div className="flex justify-between py-3" key={r.id}><div><p className="font-medium">{r.name}</p><p className="text-xs text-white/45">{r.category||"ไม่ระบุหมวดหมู่"}</p></div><span className="text-xs text-white/40">{new Date(r.created_at).toLocaleDateString("th-TH")}</span></div>)}{!recent.length&&<p className="py-8 text-center text-white/40">ยังไม่มีข้อมูลกลุ่ม</p>}</div></div></div>}
